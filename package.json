{
  "name": "dashboard-content",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard Content — Instagram creator dashboard (reels + AI analysis, Banger Hunter, AI chat, stories, video variants, calendar)",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "dev:mock": "node --import ./scripts/mock-external.mjs scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
